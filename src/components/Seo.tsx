import React from 'react'

// Legacy Seo component replaced by Head API usage. Keep a no-op
// component here to avoid lingering import errors in builds/tools.
const Seo: React.FC = () => null

export default Seo
